# MySQL connection properties
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://85.10.205.173:3306/blodatabase
spring.datasource.username=bloman33
spring.datasource.password=useruser1

# Log JPA queries
# Comment this in production
spring.jpa.show-sql=true

# Only for testing purpose - comment this in production
spring.jpa.hibernate.ddl-auto=none 

# Hibernate SQL dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect

#server.servlet.session.cookie.max-age=2m
#by default, there's no max age 


